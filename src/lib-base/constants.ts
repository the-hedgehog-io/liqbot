import { Decimal } from "./Decimal.js";

export const CRITICAL_COLLATERAL_RATIO = Decimal.from(2.0);
export const MINIMUM_COLLATERAL_RATIO = Decimal.from(1.5);
export const BFEE_LIQUIDATION_RESERVE = Decimal.from(200);
export const MINIMUM_BORROWING_RATE = Decimal.from(0.005);
export const MAXIMUM_BORROWING_RATE = Decimal.from(0.05);
export const MINIMUM_REDEMPTION_RATE = Decimal.from(0.005);
